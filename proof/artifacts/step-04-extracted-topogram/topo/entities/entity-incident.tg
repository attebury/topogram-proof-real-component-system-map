entity entity_incident {
  name "Incident"
  description "Operational incident or intake item that needs review."

  fields {
    id uuid required
    title string required
    status string required default new
    priority string required default normal
    assignee string optional
    updated_at datetime required
  }

  keys {
    primary [id]
    index [status, priority]
  }

  status active
}
